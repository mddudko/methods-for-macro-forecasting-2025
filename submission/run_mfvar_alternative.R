# Placeholder for alternative MF-VAR specification
# ---------------------------------------------------------------
# This script intentionally stops execution to indicate that the
# alternative MF-VAR workflow lives on a separate feature branch.
# When merging that branch, replace this placeholder with the real
# implementation. The baseline `run_mfvar.R` guards against the
# environment variable `MFVAR_VERSION=alternative` until this file
# is populated.
# ---------------------------------------------------------------

stop("Alternative MF-VAR workflow not yet implemented on this branch. See feature branch documentation.")
